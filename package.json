{
  "name": "yarn-workspace-sync-deps",
  "description": "A simple script for keeping dependencies are same between root and packages on Yarn workspaces",
  "version": "0.3.3",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "prepublish": "yarn lint && yarn test"
  },
  "bin": {
    "yarn-workspace-sync-deps": "index.js",
    "yarnw-sync-deps": "index.js"
  },
  "repository": "mybigday/yarn-workspace-sync-deps",
  "author": "Jhen <developer@jhen.me>",
  "license": "MIT",
  "devDependencies": {
    "@fugood/eslint-config-base": "^0.4.0",
    "eslint": "^7.0.0",
    "jest": "^29.2.0",
    "shelljs": "^0.8.1"
  },
  "dependencies": {
    "minimist": "^1.2.0"
  }
}
